import { ChangeDetectorRef, ElementRef, EventEmitter, OnInit, Renderer2 } from '@angular/core';
import { NestedListTitleDirective } from '../nested-list-directives';
import { NestedListStateService } from '../nested-list-state.service';
export declare class NestedLinkDirective implements OnInit {
    private renderer;
    private elementRef;
    private nestedListStateService;
    changeDetRef: ChangeDetectorRef;
    /** @hidden */
    fdNestedListItemClass: boolean;
    /**
     *  @hidden
     *  Reference to title element, it is used, to get title for condensed mode.
     */
    title: NestedListTitleDirective;
    /** Event that is thrown, when any keyboard event is dispatched on this element */
    readonly keyboardTriggered: EventEmitter<KeyboardEvent>;
    /** Event that is thrown, when this element is clicked */
    readonly clicked: EventEmitter<MouseEvent>;
    /** Whether this element is selected, the `selected` state is propagated to all of parent elements */
    selected: boolean;
    _selected: boolean;
    /** Function that is called on click event dispatch on this element. */
    onClickCallback: Function;
    /** */
    controlSelected: boolean;
    /**
     * @hidden
     * Attribute controlled by the parent `NestedItemDirective`
     */
    expanded: boolean;
    /**
     * @hidden
     * Attribute controlled by the parent `NestedItemDirective`
     */
    hasChildren: boolean;
    /** Set focus on the element. */
    focus(): void;
    /** Dispatches the click event on the element */
    click(): void;
    /** @hidden */
    constructor(renderer: Renderer2, elementRef: ElementRef, nestedListStateService: NestedListStateService, changeDetRef: ChangeDetectorRef);
    /** @hidden */
    ngOnInit(): void;
    /** Returns the title value of the title directive */
    getTitle(): string;
}
