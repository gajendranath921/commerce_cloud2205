import { ElementRef } from '@angular/core';
import { AbstractFdNgxClass } from '../../utils/abstract-fd-ngx-class';
export declare type BadgeStatus = 'success' | 'warning' | 'error';
export declare type BadgeModifier = 'pill' | 'filled';
export declare type BadgeIconStatus = 'available' | 'away' | 'busy' | 'offline';
/**
 * Label component, used to indicate status, without any background or border
 * Colors, generally in combination with text, are used to easily highlight the state of an object.
 */
export declare class LabelComponent extends AbstractFdNgxClass {
    private elementRef;
    /** Color coded status for the label. Options are 'success', 'warning', and 'error'. Leave empty for default label. */
    status: BadgeStatus;
    /** @hidden */
    _setProperties(): void;
    /** @hidden */
    constructor(elementRef: ElementRef);
}
