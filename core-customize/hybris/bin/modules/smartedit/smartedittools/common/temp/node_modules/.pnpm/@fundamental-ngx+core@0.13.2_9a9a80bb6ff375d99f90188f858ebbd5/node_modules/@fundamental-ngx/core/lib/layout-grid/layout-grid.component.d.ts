import { ElementRef } from '@angular/core';
import { AbstractFdNgxClass } from '../utils/abstract-fd-ngx-class';
/**
 * Use a layout grid to arrange components evenly in a grid layout.
 */
export declare class LayoutGridComponent extends AbstractFdNgxClass {
    private elementRef;
    /** @Input Column span for the grid system */
    col: number;
    /** Whether the grid should have a gap. */
    nogap: boolean;
    /** Whether the grid should have a gap. */
    gapSize: number;
    /** @hidden */
    _setProperties(): void;
    /** @hidden */
    constructor(elementRef: ElementRef);
}
